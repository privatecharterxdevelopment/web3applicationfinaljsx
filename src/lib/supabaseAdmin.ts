/**
 * CRM admin client — server-side service role only (see api/crm-admin.cjs).
 */
// eslint-disable-next-line @typescript-eslint/ban-ts-comment
// @ts-expect-error JS module
export { supabaseAdmin, default } from './supabaseAdminClient.js';
